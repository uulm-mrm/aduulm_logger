variables:
  GIT_STRATEGY: none

# Tests for Noetic with Install
noetic-catkin_build-install:
  image: mrm-git.e-technik.uni-ulm.de:4567/aduulm/docker_images/aduulm-ros-noetic:latest
  stage: build
  script:
    - build_repo_with_dependencies.sh gitlab_install
    
# Tests for Noetic without install
noetic-catkin_build-no_install:
  image: mrm-git.e-technik.uni-ulm.de:4567/aduulm/docker_images/aduulm-ros-noetic:latest
  stage: build
  script:
    - build_repo_with_dependencies.sh gitlab_no_install

# Tests for Noetic without install
rolling-colcon_build-install:
  image: mrm-git.e-technik.uni-ulm.de:4567/aduulm/docker_images/aduulm-ros2-rolling:latest
  stage: build
  script:
    - build_repo_with_dependencies_ros2.sh rolling

# Tests for Unicar Melodic with Install
unicar-melodic-catkin_build-install:
  image: mrm-git.e-technik.uni-ulm.de:4567/aduulm/docker_images/unicar-ros-melodic-ci:latest
  stage: build
  script:
    - build_repo_with_dependencies.sh gitlab_install
